<%- include('../../partials/headAndNav', {pageName: "Movies" , user:user }) %>
    <h3 class="bekind">Be kind, Unwind</h3>

    <% for(let i=0; i < media.length; i++) { %>
        <div class="col">
            <a href=/home/movies/<%=media[i]._id %> class="atag">
                <p>
                    <%= media[i].name %>
                </p>
                <div id="poster">
                    <img id="one" src=<%=media[i].img %> alt="Movie Selection">
                </div>
            </a>
            <div class="hide" id="desc">
                <%= media[i].description %>
            </div>
        </div>
        <% } %>